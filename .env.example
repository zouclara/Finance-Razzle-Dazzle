# ──────────────────────────────────────────────────────────
# Finance-Razzle-Dazzle — API Credentials
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ──────────────────────────────────────────────────────────

# ── QuickBooks (source of truth for all three statements) ──
# Get from: developer.intuit.com → My Apps → Keys & OAuth
QB_CLIENT_ID=
QB_CLIENT_SECRET=
QB_REALM_ID=          # Your company ID (visible in QBO URL)
QB_ACCESS_TOKEN=      # Short-lived; refresh via QB_REFRESH_TOKEN
QB_REFRESH_TOKEN=     # Use scripts/refresh_qb_token.py to rotate
QB_ENVIRONMENT=production  # or sandbox

# ── Stripe (revenue, MRR/ARR, payouts) ──
# Get from: dashboard.stripe.com/apikeys
# Use a Restricted Key with read-only access to: charges, subscriptions,
# balance, payouts, customers, invoices
STRIPE_SECRET_KEY=sk_live_...

# ── Mercury (banking, cash balances, transactions) ──
# Get from: app.mercury.com → Settings → API
MERCURY_API_TOKEN=

# ── Brex (corporate card, expenses, AP) ──
# Get from: dashboard.brex.com → Settings → Developer
BREX_API_TOKEN=

# ── Gusto (payroll — headcount costs, department allocation) ──
# Get from: dev.gusto.com → OAuth Apps
GUSTO_CLIENT_ID=
GUSTO_CLIENT_SECRET=
GUSTO_COMPANY_ID=
GUSTO_ACCESS_TOKEN=

# ── HubSpot (CRM — pipeline, deals, CAC context) ──
# Get from: app.hubspot.com → Settings → Integrations → Private Apps
HUBSPOT_ACCESS_TOKEN=

# ── Google Sheets (manual journal entries / FP&A supplements) ──
# Get from: console.cloud.google.com → Service Accounts
GOOGLE_SERVICE_ACCOUNT_JSON=path/to/service_account.json
GOOGLE_SHEETS_SPREADSHEET_ID=

# ── Dashboard settings ──
COMPANY_NAME="Your Company"
FISCAL_YEAR_START_MONTH=1   # 1 = January
USE_DEMO_DATA=true           # Set to false once credentials are live
